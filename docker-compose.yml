version: "3"
services:
  app:
    container_name: ${COMPOSE_PROJECT_NAME}-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${COMPOSE_APP_PORT}:3335"
    networks:
      - app-local-ws

networks:
  app-local-ws:
    driver: bridge
